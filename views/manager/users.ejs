<% layout('../layout') %>

<div class="row">
    <div class="col-md-12">
        <div class="list-group">
        <% users.forEach((user) => { %>
            <div class="list-group-item">
                <%= user.facebook.name %>
            </div>
        <% }); %>
        </div>
    </div>
</div>